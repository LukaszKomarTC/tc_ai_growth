# INC-2026-07-27 — tossacycling.com WordPress compromise (spam-mailer + RCE)

**Three-status (as of 2026-07-28, post full infrastructure verification):**
- **Operational:** Stable — site online, business as normal.
- **Security:** No active indicators of compromise; every persistence class audited clean.
- **Forensic:** All remediation, full infrastructure verification, clean baseline backup, and
  restore-test COMPLETE [evidenced]. **Only the passive monitoring window remains before
  formal CLOSED.**

**Closure evidence (2026-07-28):**
- **Clean baseline backup** created (24 GB Plesk full backup, post-remediation) — label it
  `CLEAN BASELINE post-INC 2026-07-28` to distinguish from the 19 GB compromised copy.
- **Restore-test PASSED** — the 569 MB clean DB dump imported into an isolated scratch DB with
  `exit 0`: 171 tables, 293 users, 15,718 orders, 5,670 options — full dataset intact; scratch
  DB dropped. Combined with the successful July-18 Plesk *file* restore earlier tonight, both
  file- and data-layer restorability are demonstrated.
- **Remaining:** several clean monitoring days (ideally automated via the weekly checksum-scan
  cron in the runbook), then flip to CLOSED and re-baseline the box as trusted.

## Verification results — 2026-07-28 [evidenced]

Runbook Phases 1–4 executed; all clean. The compromise was **WordPress-layer only** — it never
reached the OS, the other subscriptions, or the orchestrator.

- **1a Application passwords** — user 1: one legit 2023 WooCommerce-app password (pre-breach);
  user 119: none. No attacker app-password.
- **1b WordPress admins** — exactly ids 1, 119.
- **1c OS layer** — SSH: only the `plesk-ssh-terminal` key (home/vhost keys empty). Users: only
  Plesk subscription accounts (`/bin/false`). Logins: all `root@127.0.0.1` (Plesk terminal),
  zero external SSH. systemd: post-Jul-18 units all Plesk/dovecot (package updates), no rogue
  timer; `tc-autodeploy.timer` inactive. Crontabs: `lukasz` gone, only legit `psaadm` remains.
  **Confirms the attacker never had OS-level access** (the one malicious cron was installed via
  the web shell, not SSH).
- **2 Mail** — queue held 21 msgs: mostly deferred system mail to the owner + 2 spam remnants
  (`support@`/`admin@` spoofed → Comcast strangers). **Outbound port 25 blocked/timing out for
  all destinations** (likely IONOS policy/abuse response) — this is why the spam overwhelmingly
  failed (2702 deferred / 2 sent) and reputation survived. Business mail unaffected (IONOS SMTP
  submission :587; test mail reached Gmail inbox). Queue flushed (`postsuper -d ALL`, 21 deleted).
- **3 Other subscriptions** — `tourdegirona.com` + `zelcycling.com` scanned clean; `dashboard`
  (Laravel) and `dev` have no WP docroot. Separate users + no OS access = no pivot.
- **4 Orchestrator** `/opt/tc_ai_growth` — **code intact.** Only PHP is our own connector source;
  every `eval/base64` hit was inside `.venv` third-party libs; zero malware fingerprints; git
  tree clean except one untracked `.env.save` (dated Jul 5, pre-breach, mode 600, does NOT
  contain the old signing key). **Platform gate's hard prerequisite (orchestrator uncompromised)
  is MET.**

Original three-status line (2026-07-27) retained below for history.

**Three-status (as of 2026-07-27 late, post app-password audit):**
- **Operational:** Stable — site online, business as normal.
- **Security:** No active indicators of compromise currently identified.
- **Forensic:** Pending final infrastructure verification (runbook Phases 1c–6).

A working site proves availability, not security. Scoped findings must not be generalized:
e.g. the application-password audit proves *only* that no attacker app-password exists for
users 1/119 — it says nothing about SSH keys, systemd, mail queue, or other subscriptions.

**Status:** ACTIVE THREAT NEUTRALIZED (evidenced) — **NOT yet formally closed** (verification
pending, see the closure runbook).

## Verified clean tonight [evidenced] vs still-open [not verified]

**Verified with evidence tonight** (do not re-list these as uncertain): WordPress admin set
(only ids 1, 119); core integrity (verify-checksums Success); plugins/themes (restored from
pre-breach backup + re-swept); mu-plugins (emptied); uploads (swept); WordPress/`lukasz` cron
(malicious lines removed); application passwords (no attacker entry). **Still open** (OS/infra,
not yet examined): SSH keys, `/etc/passwd` users, systemd units/timers, all system crontabs,
mail queue + logs, the other four subscriptions (fingerprint-grepped only — insufficient),
orchestrator filesystem, DB triggers, plus the clean-baseline + restore-test + monitoring.
**Severity:** high (production e-commerce, 8-day dwell, outbound spam, RCE persistence).
**Scope:** `tossacycling.com` only, confirmed by fingerprint sweep of all five vhosts (the
other four not yet given the full checksum/timestamp treatment — see open items).

## Confidence statement

Everything below marked **[evidenced]** has supporting command output or log lines captured
during the 2026-07-27 remediation session. Items marked **[not verified]** are open and must
be closed before the incident is declared resolved. This document deliberately separates the
two — do not read "threat neutralized" as "incident closed."

## Timeline (from access logs + file timestamps) [evidenced]

| When (UTC) | Event |
|---|---|
| Jul 17 18:34 | Recon: `86.54.25.131` probes `miniorange-saml-.../readme.txt` → 404 (plugin-hunting) |
| Jul 19 06:10 | `86.54.25.131` hammers `POST /?rest_route=/batch/v1` ×15 → 207 (probe run) |
| Jul 19 06:30–06:31 | `104.196.104.214`: `POST /wp-json/batch/v1` ×10 → 207 |
| Jul 19 06:31:06 | Rogue admin `wpsvc_44c6b050b84a` created (id 426) |
| Jul 19 06:31:11 | `POST /wp-login.php` → 302 (logs in as the new admin) |
| Jul 19 06:31:20 | `POST /wp-admin/update.php?action=upload-plugin` → 200 (uploads shell ZIP) |
| Jul 19 06:31:28–31 | Shell executed (`?c=id;uname -a;...` + spam-mailer capability probe) |
| Jul 19 06:31 | 17 shell files dropped into `wp-content/mu-plugins/` (auto-load persistence) |
| Jul 19–25 | Spam Customer signups (ids 427–436; some names may be real customers — review, don't bulk-delete) |
| Jul 20 16:38 | Third-wave shell `class-wp-cache-helper-337eb7.php` (96 KB) in fake-plugin dir |
| Jul 26 22:15 | 2nd rogue admin `tossacycling_dev158a` (id 437); `wp-config.php` injection; `object-cache.php` dropper; resurrection cron installed in `lukasz` crontab |
| Jul 27 15:30–17:30 | `/tmp/phpiAgloq` self-test beacons every 30 min (curl from server to itself) |
| Jul 27 ~17:51 | Owner discovers site unresponsive. **Proximate cause was a SEPARATE, coincidental performance issue** — 9.5 MB autoload bloat from String Locator transients pegging mysqld at 300% CPU (used during migration; not attacker-related). Fixed by deleting the transient rows. |
| Jul 27 18:24 | **Self-reinfection during cleanup** — a still-live dropper re-created `class-wp-taxonomy-ee7d1b.php` ~8 min after we deleted it. Proof that in-place cleanup could not be trusted and a clean-backup restore was required. |
| Jul 27 ~18:00–21:30 | Remediation (below) |

## Entry vector [evidenced]

**WordPress REST Batch API (`/wp-json/batch/v1`).** The attacker batched requests to abuse a
vulnerable plugin route and create an admin account **without authentication**, then logged in
as it and used the normal `upload-plugin` admin capability to install a malicious plugin ZIP
containing a webshell.

- **Exact vulnerable plugin: NOT confirmed** — batch request bodies are not in access logs.
  Leading suspect is the **Gravity Forms suite** (7 GF plugins present, largest REST surface,
  includes User Registration; the WooCommerce-GF addon was outdated with a pending update).
  The fix does not depend on naming it (vector blocked + all plugins updated).
- The brute-force from `172.236.140.96` (`POST /wp-login.php`, all 200 = failed) was a
  **separate, unrelated** commodity attack — explicitly NOT the entry.

## Goal [evidenced]

**Spam relay + RCE.** The deployed shell (`class-wp-taxonomy-ee7d1b.php`) is a multi-tier spam
mailer (`$K='sm_...'`; sendmail-pipe → raw SMTP socket → PHP `mail()` fallbacks) with a hidden
`shell_exec` RCE mode. First shell commands probed mail capability (sendmail binary, port 25,
`mail()`, `disable_functions`, MX lookup). Consistent with the site's prior tobacco/pharmacy
SEO-spam history (INC-2026-02-01).

## Attribution

**Unknown and unknowable from this evidence.** We have attacker IPs (`104.196.104.214`
successful, `86.54.25.131` probe) but IPs = proxies/VPNs/compromised hosts. Pattern is an
**automated criminal exploit campaign**, not evidence of targeting Tossa Cycling specifically.

## Remediation performed [evidenced]

- **Core** reinstalled pristine; `wp core verify-checksums` → *Success* (after removing 2 shells
  hidden in `wp-includes/`: `class-wp-simplepie-file-resolver.php`, `assets/class-walker_c080dd89.php`).
- **Code** restored from the **July 18 (pre-breach) backup**, files-only, `wp-content/plugins` +
  `themes` only (kept pristine core, cleaned uploads, and current DB with orders). Restored
  files re-swept clean (fingerprint + hex-name).
- **Persistence removed** (all quarantined to `/root/quarantine/` with hashes): `lukasz` crontab
  (2 malicious lines), `object-cache.php`, wp-config injection block, DB row
  `_site_transient_health_b5592a35` (502 KB base64 payload), 12 uploads shells, 2 core shells,
  17 mu-plugins shells, fake plugin `widget-accessibility-helper-5fb784` (3 files), `/tmp/phpiAgloq`.
- **Rogue admins** 426 + 437 deleted; admin set confirmed = only ids 1, 119.
- **7 free plugins** updated to latest (6 premium pending licensed update — from clean backup, vector blocked so not an open door).
- **Hardening:** `mu-plugins/zzz-tc-security.php` disables `batch/v1`; `WP_DEBUG=false`,
  `DISALLOW_FILE_EDIT=true`; firewall deny `172.236.140.96`; autodeploy timer stopped;
  registration confirmed off (default role Customer).
- **Secrets rotated:** WP salts, DB password (wp-config synced), 2 admin passwords, SMTP
  (mailbox + `WPMS_SMTP_PASS`), `TC_GROWTH_SIGNING_KEY` (wp-config + both orchestrator files, perms preserved).
- **Email:** test delivered to Gmail **inbox** → reputation likely intact (low spam volume or short window).
- **Backups preserved:** 569 MB DB dump + 19 GB full current-state (both pre-restore).

## Open items — required before formal closure [not verified]

Persistence vectors we did NOT audit tonight (highest re-entry risk first):

1. **WordPress application passwords** — survive password/salt rotation; must be enumerated + revoked.
2. **SSH `authorized_keys`, `/etc/passwd` new users, systemd units/timers** — only crontab was reviewed.
3. **Outbound mail queue + mail logs** — mailer files removed, but queue not flushed/inspected.
4. **Other 4 subscriptions** (dashboard/tourdegirona/dev.tourdegirona/zelcycling) — only fingerprint-grepped, which we proved misses variants; need full checksum/timestamp pass.
5. **Orchestrator** `/opt/tc_ai_growth` (`tcgrowth` user) — signing key rotated but filesystem not scanned.
6. **DB triggers/stored routines** — only `wp_options` content was scanned.
7. **Clean baseline** created *after* verification; **restore-test** on staging; **several clean days** of monitoring.

## Corrections to earlier advice

- The 19 GB compromised backup does **not** "cost nothing." It holds live malware and possibly
  customer PII. Label `DO_NOT_RESTORE-COMPROMISED-2026-07-27`, `sha256sum` + record, `chmod 600`,
  set a deletion date (~2 weeks), and **exclude from operational backups**. Same for `/root/quarantine/`.

## Platform impact

Release 0.3 gate **remains suspended** — the VPS hosts the orchestrator and connector, and was
under attacker control today. Do not resume the integration programme until this incident is
formally closed (open items above) and the box is re-baselined as trusted.
